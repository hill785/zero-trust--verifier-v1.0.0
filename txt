# .gitignore - Optimized for ATPT Native Binary Packaging V1.0.0# Deactivate and wipe the legacy virtual workspace environment
deactivate 2>/dev/null || true
rm -rf venv/

# Re-initialize clean environment paths
python3 -m venv venv
source venv/bin/activate

# Provision the production-proven toolchain
python -m pip install --upgrade pip
pip install -r requirements.txt

# Run the un-truncated zero-source audit validation pipeline script
./verify_package.sh

